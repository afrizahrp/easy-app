import { ReactNode } from 'react';
export const metadata = {
  title: 'Sales Person Performa Analytics',
};
const SalesAnaylytics = ({ children }: { children: ReactNode }) => {
  return <>{children}</>;
};

export default SalesAnaylytics;
