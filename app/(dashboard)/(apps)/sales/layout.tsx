import { ReactNode } from 'react';
export const metadata = {
  title: 'Sales Invoice ',
};
const SalesInvoiceLayout = ({ children }: { children: ReactNode }) => {
  return <>{children}</>;
};

export default SalesInvoiceLayout;
