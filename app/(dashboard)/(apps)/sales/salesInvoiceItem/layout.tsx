import { ReactNode } from 'react';
export const metadata = {
  title: 'Sales Invoice Detail',
};
const SalesInvoiceDtLayout = ({ children }: { children: ReactNode }) => {
  return <>{children}</>;
};

export default SalesInvoiceDtLayout;
